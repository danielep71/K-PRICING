# 📂 Classes

This directory is reserved for K-PRICING's application profile. Add authoritative exported source here when the project needs this component role. The supported façade (`src/modules/KPR_DATES_DAYS.bas`), internal cores (`src/core/`) and regression harness (`tests/modules/KPR_REGRESSION_TESTS.bas`) already exist in their governed locations; no class modules exist yet.
